services:
  conducteur:
    image: yassinsiouda/conducteur:dev
    build: ./orchestrator
    ports:
      - "8000:80"
    env_file:
      - .env
    volumes:
      - ./data/id_rsa/conducteur:/root/.ssh/id_rsa
      - ./orchestrator/src/:/app/src/
      - ./data:/app/data
  dashboard:
    profiles:
      - dev
    build: ./dashboard
    ports:
      - "5173:5173"
    env_file:
      - .env
    volumes:
      - ./dashboard/src:/app/src